package com.codelab.android.datastore.ui.theme

import androidx.compose.material.Colors
import androidx.compose.material.MaterialTheme
import androidx.compose.ui.graphics.Color

/**
 * Used for the [Colors.primary] color of the `DarkColorPalette` used by our [DataStoreTheme]
 * custom [MaterialTheme]
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * Used for the [Colors.primary] color of the `LightColorPalette` used by our [DataStoreTheme]
 * custom [MaterialTheme]
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * Used for the [Colors.primaryVariant] color of both the `LightColorPalette` and `DarkColorPalette`
 * used by our [DataStoreTheme] custom [MaterialTheme]
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * Used for the [Colors.secondary] color of both the `LightColorPalette` and `DarkColorPalette`
 * used by our [DataStoreTheme] custom [MaterialTheme]
 */
val Teal200: Color = Color(0xFF03DAC5)