package androidx.demo.motiondemos.ui.theme

import androidx.compose.material.Colors
import androidx.compose.material.MaterialTheme
import androidx.compose.material.darkColors
import androidx.compose.material.lightColors
import androidx.compose.ui.graphics.Color

/**
 * Used as the [Colors.primary] of the [darkColors] used by the [MotionDemosTheme] custom [MaterialTheme]
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * Used as the [Colors.primary] of the [lightColors] used by the [MotionDemosTheme] custom [MaterialTheme]
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * Used as the [Colors.primaryVariant] of both the [darkColors] and the [lightColors] used by the
 * [MotionDemosTheme] custom [MaterialTheme]
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * Used as the [Colors.secondary] of both the [darkColors] and the [lightColors] used by the
 * [MotionDemosTheme] custom [MaterialTheme]
 */
val Teal200: Color = Color(0xFF03DAC5)