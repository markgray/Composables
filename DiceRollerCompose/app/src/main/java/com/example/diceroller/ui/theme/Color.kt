package com.example.diceroller.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * A light purple
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * A medium purple
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * A dark purple
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * A dark blue-green color. Its name comes from that of a bird — the Eurasian teal (Anas crecca)
 * which presents a similarly colored stripe on its head. The word is often used colloquially to
 * refer to shades of cyan in general.
 */
val Teal200: Color = Color(0xFF03DAC5)