package android.support.composegraph3d

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    /**
     * This test checks if the addition of 2 + 2 equals 4.
     * It uses the assertEquals function from the org.junit.Assert library
     * to compare the expected value (4) with the actual value (2 + 2).
     * If the values are not equal, the test will fail.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}