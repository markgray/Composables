package com.codelab.android.trackmysleepquality.ui.theme

import androidx.compose.material.Colors
import androidx.compose.material.MaterialTheme
import androidx.compose.ui.graphics.Color

/**
 * This is used as the [Colors.primary] color of the `DarkColorPalette` of our
 * [TrackMySleepQualityTheme] custom [MaterialTheme].
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * This is used as the [Colors.primary] color of the `LightColorPalette` of our
 * [TrackMySleepQualityTheme] custom [MaterialTheme].
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * This is used as the [Colors.primaryVariant] color of both the `DarkColorPalette` and the
 * `LightColorPalette` of our [TrackMySleepQualityTheme] custom [MaterialTheme].
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * This is used as the [Colors.secondary] color of both the `DarkColorPalette` and the
 * `LightColorPalette` of our [TrackMySleepQualityTheme] custom [MaterialTheme].
 */
val Teal200: Color = Color(0xFF03DAC5)