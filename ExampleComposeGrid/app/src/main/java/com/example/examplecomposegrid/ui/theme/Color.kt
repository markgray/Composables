package com.example.examplecomposegrid.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Used as the `primary` color of the `DarkColorPalette`
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * Used as the `primary` color of the `LightColorPalette`
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * Used as the `primaryVariant` color of both the `DarkColorPalette` and the `LightColorPalette`
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * Used as the `secondary` color of both the `DarkColorPalette` and the `LightColorPalette`
 */
val Teal200: Color = Color(0xFF03DAC5)