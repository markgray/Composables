package com.example.androidtriviacompose.ui.theme

import androidx.compose.material.darkColors
import androidx.compose.material.lightColors
import androidx.compose.ui.graphics.Color

/**
 * The [Color] used for the `primary` color of [darkColors]
 */
val Purple200: Color = Color(0xFFBB86FC)

/**
 * The [Color] used for the `primary` color of [lightColors]
 */
val Purple500: Color = Color(0xFF6200EE)

/**
 * The [Color] used for the `primaryVariant` color of both [darkColors] and [lightColors]
 */
val Purple700: Color = Color(0xFF3700B3)

/**
 * The [Color] used for the `secondary` color of both [darkColors] and [lightColors]
 */
val Teal200: Color = Color(0xFF03DAC5)