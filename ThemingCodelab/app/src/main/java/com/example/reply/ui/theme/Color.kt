/*
 * Copyright 2022 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

@file:Suppress("unused")

package com.example.reply.ui.theme

import androidx.compose.material3.ColorScheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.ui.graphics.Color

/**
 * Used as the [ColorScheme.primary] of our [lightColorScheme].
 */
val md_theme_light_primary: Color = Color(0xFF825500)

/**
 * Used as the [ColorScheme.onPrimary] of our [lightColorScheme].
 */
val md_theme_light_onPrimary: Color = Color(0xFFFFFFFF)

/**
 * Used as the [ColorScheme.primaryContainer] of our [lightColorScheme].
 */
val md_theme_light_primaryContainer: Color = Color(0xFFFFDDB3)

/**
 * Used as the [ColorScheme.onPrimaryContainer] of our [lightColorScheme].
 */
val md_theme_light_onPrimaryContainer: Color = Color(0xFF291800)

/**
 * Used as the [ColorScheme.secondary] of our [lightColorScheme].
 */
val md_theme_light_secondary: Color = Color(0xFF6F5B40)

/**
 * Used as the [ColorScheme.onSecondary] of our [lightColorScheme].
 */
val md_theme_light_onSecondary: Color = Color(0xFFFFFFFF)

/**
 * Used as the [ColorScheme.secondaryContainer] of our [lightColorScheme].
 */
val md_theme_light_secondaryContainer: Color = Color(0xFFFBDEBC)

/**
 * Used as the [ColorScheme.onSecondaryContainer] of our [lightColorScheme].
 */
val md_theme_light_onSecondaryContainer: Color = Color(0xFF271904)

/**
 * Used as the [ColorScheme.tertiary] of our [lightColorScheme].
 */
val md_theme_light_tertiary: Color = Color(0xFF51643F)

/**
 * Used as the [ColorScheme.onTertiary] of our [lightColorScheme].
 */
val md_theme_light_onTertiary: Color = Color(0xFFFFFFFF)

/**
 * Used as the [ColorScheme.tertiaryContainer] of our [lightColorScheme].
 */
val md_theme_light_tertiaryContainer: Color = Color(0xFFD4EABB)

/**
 * Used as the [ColorScheme.onTertiaryContainer] of our [lightColorScheme].
 */
val md_theme_light_onTertiaryContainer: Color = Color(0xFF102004)

/**
 * Used as the [ColorScheme.error] of our [lightColorScheme].
 */
val md_theme_light_error: Color = Color(0xFFBA1A1A)

/**
 * Used as the [ColorScheme.errorContainer] of our [lightColorScheme].
 */
val md_theme_light_errorContainer: Color = Color(0xFFFFDAD6)

/**
 * Used as the [ColorScheme.onError] of our [lightColorScheme].
 */
val md_theme_light_onError: Color = Color(0xFFFFFFFF)

/**
 * Used as the [ColorScheme.onErrorContainer] of our [lightColorScheme].
 */
val md_theme_light_onErrorContainer: Color = Color(0xFF410002)

/**
 * Used as the [ColorScheme.background] of our [lightColorScheme].
 */
val md_theme_light_background: Color = Color(0xFFFFFBFF)

/**
 * Used as the [ColorScheme.onBackground] of our [lightColorScheme].
 */
val md_theme_light_onBackground: Color = Color(0xFF1F1B16)

/**
 * Used as the [ColorScheme.surface] of our [lightColorScheme].
 */
val md_theme_light_surface: Color = Color(0xFFFFFBFF)

/**
 * Used as the [ColorScheme.onSurface] of our [lightColorScheme].
 */
val md_theme_light_onSurface: Color = Color(0xFF1F1B16)

/**
 * Used as the [ColorScheme.surfaceVariant] of our [lightColorScheme].
 */
val md_theme_light_surfaceVariant: Color = Color(0xFFF0E0CF)

/**
 * Used as the [ColorScheme.onSurfaceVariant] of our [lightColorScheme].
 */
val md_theme_light_onSurfaceVariant: Color = Color(0xFF4F4539)

/**
 * Used as the [ColorScheme.outline] of our [lightColorScheme].
 */
val md_theme_light_outline: Color = Color(0xFF817567)

/**
 * Used as the [ColorScheme.inverseOnSurface] of our [lightColorScheme].
 */
val md_theme_light_inverseOnSurface: Color = Color(0xFFF9EFE7)

/**
 * Used as the [ColorScheme.inverseSurface] of our [lightColorScheme].
 */
val md_theme_light_inverseSurface: Color = Color(0xFF34302A)

/**
 * Used as the [ColorScheme.inversePrimary] of our [lightColorScheme].
 */
val md_theme_light_inversePrimary: Color = Color(0xFFFFB951)

/**
 * Could be used as the `ColorScheme.shadow` of our [lightColorScheme] it only it existed.
 */
val md_theme_light_shadow: Color = Color(0xFF000000)

/**
 * Used as the [ColorScheme.surfaceTint] of our [lightColorScheme].
 */
val md_theme_light_surfaceTint: Color = Color(0xFF825500)

/**
 * Used as the [ColorScheme.outlineVariant] of our [lightColorScheme].
 */
val md_theme_light_outlineVariant: Color = Color(0xFFD3C4B4)

/**
 * Used as the [ColorScheme.scrim] of our [lightColorScheme].
 */
val md_theme_light_scrim: Color = Color(0xFF000000)

/**
 * Used as the [ColorScheme.primary] of our [darkColorScheme].
 */
val md_theme_dark_primary: Color = Color(0xFFFFB951)

/**
 * Used as the [ColorScheme.onPrimary] of our [darkColorScheme].
 */
val md_theme_dark_onPrimary: Color = Color(0xFF452B00)

/**
 * Used as the [ColorScheme.primaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_primaryContainer: Color = Color(0xFF633F00)

/**
 * markgray  Used as the [ColorScheme.onPrimaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_onPrimaryContainer: Color = Color(0xFFFFDDB3)

/**
 * Used as the [ColorScheme.secondary] of our [darkColorScheme].
 */
val md_theme_dark_secondary: Color = Color(0xFFDDC2A1)

/**
 * Used as the [ColorScheme.onSecondary] of our [darkColorScheme].
 */
val md_theme_dark_onSecondary: Color = Color(0xFF3E2D16)

/**
 * Used as the [ColorScheme.secondaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_secondaryContainer: Color = Color(0xFF56442A)

/**
 * Used as the [ColorScheme.onSecondaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_onSecondaryContainer: Color = Color(0xFFFBDEBC)

/**
 * Used as the [ColorScheme.tertiary] of our [darkColorScheme].
 */
val md_theme_dark_tertiary: Color = Color(0xFFB8CEA1)

/**
 * Used as the [ColorScheme.onTertiary] of our [darkColorScheme].
 */
val md_theme_dark_onTertiary: Color = Color(0xFF243515)

/**
 * Used as the [ColorScheme.tertiaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_tertiaryContainer: Color = Color(0xFF3A4C2A)

/**
 * Used as the [ColorScheme.onTertiaryContainer] of our [darkColorScheme].
 */
val md_theme_dark_onTertiaryContainer: Color = Color(0xFFD4EABB)

/**
 * Used as the [ColorScheme.error] of our [darkColorScheme].
 */
val md_theme_dark_error: Color = Color(0xFFFFB4AB)

/**
 * Used as the [ColorScheme.errorContainer] of our [darkColorScheme].
 */
val md_theme_dark_errorContainer: Color = Color(0xFF93000A)

/**
 * Used as the [ColorScheme.onError] of our [darkColorScheme].
 */
val md_theme_dark_onError: Color = Color(0xFF690005)

/**
 * Used as the [ColorScheme.onErrorContainer] of our [darkColorScheme].
 */
val md_theme_dark_onErrorContainer: Color = Color(0xFFFFDAD6)

/**
 * Used as the [ColorScheme.background] of our [darkColorScheme].
 */
val md_theme_dark_background: Color = Color(0xFF1F1B16)

/**
 * Used as the [ColorScheme.onBackground] of our [darkColorScheme].
 */
val md_theme_dark_onBackground: Color = Color(0xFFEAE1D9)

/**
 * Used as the [ColorScheme.surface] of our [darkColorScheme].
 */
val md_theme_dark_surface: Color = Color(0xFF1F1B16)

/**
 * Used as the [ColorScheme.onSurface] of our [darkColorScheme].
 */
val md_theme_dark_onSurface: Color = Color(0xFFEAE1D9)

/**
 * Used as the [ColorScheme.surfaceVariant] of our [darkColorScheme].
 */
val md_theme_dark_surfaceVariant: Color = Color(0xFF4F4539)

/**
 * Used as the [ColorScheme.onSurfaceVariant] of our [darkColorScheme].
 */
val md_theme_dark_onSurfaceVariant: Color = Color(0xFFD3C4B4)

/**
 * Used as the [ColorScheme.outline] of our [darkColorScheme].
 */
val md_theme_dark_outline: Color = Color(0xFF9C8F80)

/**
 * Used as the [ColorScheme.inverseOnSurface] of our [darkColorScheme].
 */
val md_theme_dark_inverseOnSurface: Color = Color(0xFF1F1B16)

/**
 * Used as the [ColorScheme.inverseSurface] of our [darkColorScheme].
 */
val md_theme_dark_inverseSurface: Color = Color(0xFFEAE1D9)

/**
 * Used as the [ColorScheme.inversePrimary] of our [darkColorScheme].
 */
val md_theme_dark_inversePrimary: Color = Color(0xFF825500)

/**
 * Could be used as the `ColorScheme.shadow` of our [darkColorScheme] it only it existed.
 */
val md_theme_dark_shadow: Color = Color(0xFF000000)

/**
 * Used as the [ColorScheme.surfaceTint] of our [darkColorScheme].
 */
val md_theme_dark_surfaceTint: Color = Color(0xFFFFB951)

/**
 * Used as the [ColorScheme.outlineVariant] of our [darkColorScheme].
 */
val md_theme_dark_outlineVariant: Color = Color(0xFF4F4539)

/**
 * Used as the [ColorScheme.scrim] of our [darkColorScheme].
 */
val md_theme_dark_scrim: Color = Color(0xFF000000)


/**
 * Used as the seed of our current [ColorScheme] (maybe?).
 */
val seed: Color = Color(0xFF825500)
